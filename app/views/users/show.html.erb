<h1><%= @user.login %>'s page</h1>
<h2><%= @user.login %>'s pseuds</h2>
<% @user.pseuds.each do |pseud| %>
  <%= link_to pseud.name, [@user, pseud] %>
<% end %>
<h2><%= link_to "Support tickets opened by #{@user.login}", user_support_tickets_path(@user) %></h2>
